# AGENTS

## Reglas operativas

- Para levantar, parar, reiniciar o inspeccionar servicios Docker en este proyecto, usa siempre los objetivos de `Makefile`.
- No ejecutes `docker compose ...` manualmente salvo diagnóstico puntual cuando no exista un objetivo equivalente en `Makefile`.
